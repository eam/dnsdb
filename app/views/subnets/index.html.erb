<h1>Listing subnets</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @subnets.each do |subnet| %>
  <tr>
    <td><%= subnet.base %>/<%= subnet.mask_bits %></td>
    <td><%= link_to 'Show', subnet %></td>
    <td><%= link_to 'Destroy', subnet, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Subnet', new_subnet_path %>
